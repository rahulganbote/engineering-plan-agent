# ────────────────────────────────────────────────────────────────────────────
# EM Copilot — BRD to Engineering Plan Agent
# requirements.txt — pinned production dependencies
# Compatibility python==3.11
# Install: pip install -r requirements.txt
# ────────────────────────────────────────────────────────────────────────────

# ── Core Agent Framework ──────────────────────────────────────────────────────
langgraph==0.2.28           # LangGraph StateGraph + Send API for hub-and-spoke
langchain==0.3.7            # LangChain tools and integrations
langchain-openai==0.2.6     # LangChain OpenAI provider
openai==1.54.0              # OpenAI API client (GPT-4o + embeddings)

# ── RAG / Vector Store ────────────────────────────────────────────────────────
pinecone==5.4.2             # Pinecone serverless vector DB
tiktoken==0.8.0             # Token counting for chunking

# ── Observability ─────────────────────────────────────────────────────────────
langsmith==0.1.147          # Primary: LangGraph auto-instrumentation
langfuse==2.53.3            # Secondary: structured eval scoring + logs

# ── API Layer ─────────────────────────────────────────────────────────────────
fastapi==0.115.4            # REST API gateway
uvicorn[standard]==0.32.0   # ASGI server
python-multipart==0.0.12    # File upload support
sse-starlette==2.1.3        # Server-Sent Events for real-time streaming

# ── UI Layer ──────────────────────────────────────────────────────────────────
streamlit==1.40.0           # Web UI
plotly==5.24.1              # Eval score charts (radar, before/after)

# ── External Integrations ─────────────────────────────────────────────────────
gspread==6.1.4              # Google Sheets write action
google-auth==2.36.0         # Google service account auth
google-auth-oauthlib==1.2.1 # OAuth for Google APIs
elevenlabs==1.9.0           # ElevenLabs Conversational AI voice agent
requests==2.32.3            # HTTP client for GitHub API tool call

# ── Data / Validation ─────────────────────────────────────────────────────────
pydantic>=2.11.0,<3.0.0     # Agent output contracts — >=2.11 required by the mcp SDK
pydantic-settings==2.6.1    # Settings management from .env
pandas==2.2.3               # Eval results dataframes
reportlab==4.2.5            # PDF artifact export

# ── Document Parsing ──────────────────────────────────────────────────────────
pypdf==5.1.0                # PDF text extraction (Security Validator)
python-docx==1.1.2          # DOCX text extraction (Security Validator)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1        # .env file loading
loguru==0.7.2               # Structured logging
tenacity==9.0.0             # Retry logic (exponential backoff on LLM calls)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3               # Test runner
pytest-asyncio==0.24.0      # Async test support

# ─────────────Issues and Fixes───────────────────────────────────────────────────────────────
httpx==0.27.2



# ── Model Context Protocol (Jira Epic creation via MCP server) ──
mcp>=1.1.0                  # MCP client SDK (ClientSession, stdio_client)
mcp-atlassian>=0.11.0       # MCP server for Jira/Confluence — run as stdio subprocess
